version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///storage/db.sqlite
      - API_KEYS=dev_key_123
    volumes:
      - ./storage:/app/storage
